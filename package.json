{
  "name": "sowel-plugin-guest-access",
  "version": "1.2.0",
  "description": "Gate access for the guests of the gîte and the lodge, held in the house — Sowel plugin",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "pack": "npm run build && tar -czf sowel-plugin-guest-access-$npm_package_version.tar.gz manifest.json package.json dist ui"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.1.9"
  },
  "license": "AGPL-3.0"
}
